import { ImgObject } from "../../../GameEngine/Object/ImgObject";

export class PlayBtn extends ImgObject {
    constructor(x: number, y: number, w: number, h: number, name: string) {
        super(x, y, w, h, name);
    }

}